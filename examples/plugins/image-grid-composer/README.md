# 图片排序排版工具

打开一个本地 Tk 窗口，把图片或图片文件夹加入列表，按文件名自然排序后合成为黑底网格图。

## 使用方式

- 在 PM Center 工具栏的插件菜单中打开，手动添加图片或文件夹。
- 在文件区右键选中一张或多张图片，通过“图片工具 / 图片排序排版工具”打开，选中的图片会自动进入列表。

## 依赖

- `Pillow` 用于读取、缩放和保存图片。
- `tkinterdnd2` 用于拖拽导入；未安装时仍可通过按钮添加图片。

如果运行时提示缺少依赖，请到设置里的插件面板安装该插件依赖。
