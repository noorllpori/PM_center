# PM Center

PM Center 是一个基于 `Tauri v2 + React 19 + Rust` 的桌面项目管理器，目标是把项目浏览、脚本任务、Python 环境、设备协作和轻量编辑能力放到一个应用里。

设备协作同时支持无需服务器的局域网 UDP 发现/TCP 直连，以及可选的独立 PMC Server。Linux 服务端、Docker Compose、systemd 和 Caddy 示例位于 [`server/`](server/README.md)。

作者：章知涵@Ninniku
联系：QQ：2143178235

Windows MSI 安装包会在系统“已安装的应用”中显示发布者“章知涵@Ninniku”和对应的软件描述。

当前架构、模块边界、项目数据目录和固定交互规则请看 [项目总览与开发约定](./docs/PROJECT_CONTEXT.md)。新增功能应以该文档为准，避免在不同页面重复实现同一套文件、缓存、渲染或选择器逻辑。

项目目前仍在开发中，但已经有比较完整的主架构和一批可继续迭代的核心模块。当前实现看起来是以 Windows 使用场景为主，同时保留了 Tauri 跨平台应用的整体结构。

### 简易操作说明

- 目录中的文件可以在软件内拖拽移动，右键有简单的操作和信息。
- 支持PSD预览
- 点击文件的左半边是软件内操作，右半边按钮是模拟系统拖拽文件。
- 单击右键是程序的操作菜单，快速右键两次打开系统文件的菜单
- ctrl +alt +s 可以从任意位置唤醒主窗口显示在最前端

## TODO

此清单按当前代码和已验证功能整理。`[x]` 表示功能已经可用，`[ ]` 表示仍需实现、稳定性验收或明确产品设计；不再将“已有基础代码”直接视为完成。

### 已完成

- [x] 项目工作区：目录树、列表/网格、搜索、排序、列配置、标签、多选、内置与系统拖放、冲突处理、复制/剪切/粘贴、右键菜单和系统拖出。
- [x] 工作区与桌面集成：文件/目录/图片/视频/文本/Blender/集合/缓存/渲染标签、会话恢复、独立窗口、托盘、单实例、开机自启与 `Ctrl+Alt+S` 全局唤醒。
- [x] 智能剪贴板：功能中心或全局 `Ctrl+\`` 唤起独立 Win32 窗口，持久记录文本、图像和文件/文件夹，支持搜索、键盘选择、恢复与自动粘贴。
- [x] `.pm_center` 项目运行数据：`data.db`、目录树索引、解析缓存、缩略图缓存、排除规则和活动项目 Watcher。
- [x] 缓存管理中心：分类统计、快速/深度检查、清理缩略图和解析缓存、原子重建目录树、进度与取消；受保护数据不会被自动删除。
- [x] 内置项目文件选择器：支持文件或目录、单选或多选、扩展名过滤，并可切换到系统文件选择器。
- [x] 预览：图片、PSD、常见视频、文本/Markdown、Blender 基础信息和目录；文本、图片、视频可在独立窗口打开并回归项目标签页。
- [x] MDT 待办：关联文件、日志/多媒体、按时间创建、总览和 `Ctrl+Shift+M` 快速创建。
- [x] 集合与图片序列：手工集合的创建、添加、移除、重命名和拖入；图片序列自动分组为虚拟文件，支持逐帧播放预览。
- [x] Python 与脚本：PMC 内置/系统/Blender Python、环境检测、venv、pip、项目脚本、任务队列、日志、进度、取消和重试。
- [x] 插件基础：发现、校验、启停、依赖安装、参数和文件输入、项目设置、工具栏及文件右键动作。
- [x] Blender 渲染中心：批次、作业、帧队列、预设、常驻 Worker/逐帧兼容模式、单任务并发、性能采样、ETA、帧预览、失败重试、排序、跳过及源文件变更保护。
- [x] 渲染输出打包：使用全局配置或系统 PATH 中的 FFmpeg 将任务/批次帧序列打包为 MP4、MOV 或 WebM，并输出到项目 `renders/`。
- [x] 局域网基础：UDP 发现、在线列表和 TCP 消息收发。

### 正在验证与修正

- [ ] 导入、复制和大量文件目录的性能：继续检查拖拽导入耗时、首图冷启动和大文件目录卡顿。
- [ ] 缩略图与解析缓存命中率：缓存已落地，但需要用真实项目确认首次生成、失效和重新浏览的性能。
- [ ] 缓存恢复与项目预检：打开项目时对缺失目录、损坏/不完整 `.pm_center` 提供可靠的自动检索、手动指引和恢复流程。
- [ ] Blender 常驻 Worker 稳定性：持续验证 GPU 多开、渐进启动、驱动崩溃降级、暂停/取消、重试、排序和源文件变化场景。
- [ ] 渲染视频打包体验：缺帧占位、进度、取消、输出提示及多格式真实工程验收。
- [ ] 局域网 P2P 稳定性：修复不同设备之间在线状态不同步、反向消息不可见等问题，再扩展传输能力。
- [ ] 全局右键体验：项目工作区已拦截浏览器原生右键，继续检查独立窗口和特殊控件是否仍有遗漏。
- [ ] 插件接口：逐步开放受权限约束的文件管理、详情面板和文件窗口嵌入接口，补齐发布/更新流程。
- [ ] 项目变更记录：现有数据库已记录文件变化，需补充可浏览、筛选和调试用的界面。

### 待规划

- [ ] 新建普通文件模板与项目级快速创建流程。
- [ ] Windows 窗口句柄监听，以及以此为基础的文件星链：单向/双向链接、拓扑视图与目录内关联查找。
- [ ] 局域网二维码文件传输、权限确认、断点/失败恢复，以及后续协同或渲染农场。
- [ ] 云端登录、云端设置同步和插件商城服务端。
- [ ] 快速水印插件示例。
- [ ] MDX 支持与 MDT 编辑器体验优化。
- [ ] HDR/EXR 预览与专用工作区标签，图片轻量编辑保存。
- [ ] ZIP/RAR 预览、编辑和压缩打包。
- [ ] Blender 预览缩略图、可旋转预览格式和关联资产分析。
- [ ] 通用视频编辑、转码/压缩工具和多媒体工作流。

### 已知问题

- [ ] 大尺寸文件、批量导入和首张预览仍可能出现明显等待。
- [ ] 局域网 P2P 在部分设备组合下存在双向发现/消息异常，未达到可依赖协作的标准。
- [ ] 多 Worker 渲染受显卡显存和驱动影响，部分场景需要降至单 Worker；应优先验证实际项目再提高并发。

## 最近更新

- 功能中心新增“智能剪贴板”：历史最多 500 条、保留 30 天，数据保存在应用数据目录；`Enter` 恢复并粘贴，`Ctrl+Enter` 仅恢复，`Delete` 删除，`Esc` 关闭。
- 工作区标签右键菜单：image/video/text 标签支持直接“在独立窗口打开”，logs 仅保留关闭动作。
- 文本标签支持未保存内容的无损迁移：transfer 握手（ready/payload/ack）+ hidden window 初始化，携带 content/originalContent/language/isDirty。
- 独立图片/视频/文本窗口新增“回归项目标签页”按钮，成功后自动回到对应项目并尝试关闭独立窗口。
- Tauri 能力集补全 `core:window:allow-close`/`allow-destroy`，并将 `video-player-*` 窗口纳入 capability，解决回归后独立窗口无法关闭的问题。

## 当前已实现的方向

- 项目打开与初始化：支持打开本地项目，并在项目目录下初始化 `.pm_center` 运行数据目录
- 文件管理：目录树、文件列表、文件详情、搜索、排序、列配置
- 标签与元数据：通过 SQLite 保存标签、文件标签和变更信息
- 脚本任务：内置 Python 任务运行、日志输出、任务队列、取消与重试
- 进度解析：支持在脚本输出中使用 `/***50*/` 这类标记更新任务进度
- Python 环境管理：检测系统 Python、扫描应用 venv、创建/删除虚拟环境、安装/卸载包
- P2P 局域网通信：发现在线用户并发送消息
- 多窗口系统：内置窗口管理、代码编辑窗口、图片查看窗口
- 桌面集成：系统托盘、单实例、后台扫描等 Tauri 桌面能力

## 技术栈

- 前端：React 19、TypeScript、Vite、Tailwind CSS、Zustand
- 编辑器：CodeMirror 6
- 桌面壳：Tauri v2
- 后端：Rust、Tokio
- 数据存储：SQLite（`rusqlite` bundled）

## 目录结构

```text
pm_center/
├── src/                     # React 前端
│   ├── components/          # UI 与业务组件
│   ├── stores/              # Zustand 状态管理
│   ├── types/               # 类型定义
│   └── App.tsx              # 应用入口
├── src-tauri/               # Tauri / Rust 后端
│   └── src/
│       ├── db/              # SQLite 与元数据
│       ├── fs/              # 文件系统能力
│       ├── p2p/             # 局域网通信
│       ├── python/          # Python/Blender 相关调用
│       ├── python_env/      # Python 环境管理
│       ├── smart_clipboard/ # Windows 原生剪贴板历史窗口
│       ├── task/            # 任务执行
│       └── watcher/         # 文件变更监听
└── AGENTS.md                # 当前项目的开发说明
```

## 本地开发

开始前请先准备：

- Node.js 与 npm
- Rust stable toolchain
- Tauri 对应平台的系统依赖

安装依赖：

```bash
npm install
```

启动开发环境：

```bash
npm run tauri dev
```

构建前端：

```bash
npm run build
```

构建桌面应用：

```bash
npm run tauri build
```

## 使用说明

- 首次打开一个项目时，后端会初始化该项目的 `.pm_center/` 目录
- 这个目录会保存脚本、数据库和部分运行时信息，通常不建议提交到业务项目仓库
- Python 任务输出里如果包含 `/***N*/`，界面会把它当作 `0-100` 的进度更新


## 后续可以继续做的方向

- 更完整的项目列表与最近项目管理
- 更成熟的窗口系统与编辑器集成
- 更稳定的任务编排和日志查看
- Blender / 渲染工作流专项能力
- 文件预览、缩略图和缓存体系

## License

本项目当前按 `GPL-2.0-only` 提供，详见 [LICENSE](./LICENSE)。
