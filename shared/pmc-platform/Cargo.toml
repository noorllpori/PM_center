[package]
name = "pmc-platform"
version = "1.0.0"
edition = "2021"
license = "GPL-2.0-only"

[dependencies]
schemars = "0.8"
semver = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

